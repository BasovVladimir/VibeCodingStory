# health — правила проекта для архитектора

Личный ассистент по здоровью: заключения врачей, анализы, Apple Health и выгрузка браслета
копятся в БД и превращаются в единую картину — подсказки по еде, спорту и **вопросам, которые
надо задать врачу**. Отвечать PO **по-русски**. Видение — `docs/PRODUCT_PLAN.md`, статусы
эпиков — `docs/План и реализация.md`.

> Этот файл — свод правил для **любого** архитектора (Claude Opus · Kimi K3 · Qwen 3.8Max), не
> только для Claude; имя историческое. Точка входа для не-Claude агентов — `AGENTS.md`.
> Здесь только то, что нужно в КАЖДОЙ сессии. Всё остальное — по карте ниже, открывать ПО ЗАДАЧЕ.

## 🧭 Карта документов

**Раскладка:** корень — разводящие (`CLAUDE.md`, `AGENTS.md`) · `docs/practices/` — работа
агентов и разработка · `docs/наряды/` — задания кодеру · `docs/adr/` — архитектурные решения ·
`docs/` — видение, план, архитектура.

| Задача | Документ |
|---|---|
| **РАЗРАБОТКА И РАБОТА АГЕНТОВ** (`docs/practices/`) | |
| Кто что делает, три архитектора, распределение, поток эпиков | `docs/practices/Роли и воркфлоу.md` |
| Отдаю код кодеру: гейт, наряд, автономный цикл | `docs/practices/coder-gate.md` — переносимая практика; механика проекта — `docs/наряды/README.md` |
| Пишу или правлю код | `docs/practices/код-и-тесты.md` — комментарии, тесты, окружение, стек |
| Почему правило поведения именно такое | `docs/practices/уроки-агента.md` |
| **ПРОЕКТ** | |
| Разбираю меддокумент (ритуал «разбери») | `Промпты/Разбор документов.md` — метод, ревью-гейт, гигиена папок |
| Видение продукта, этапы, открытые вопросы | `docs/PRODUCT_PLAN.md` |
| Статусы эпиков / задания кодеру | `docs/План и реализация.md` · `docs/наряды/` |
| Как устроена система | `docs/ARCHITECTURE.md` · `docs/adr/` |

## ⚠️ КРИТИЧНО: два продуктовых инварианта (это медданные)

1. **Все назначения — только со врачом.** Сервис НЕ ставит диагнозы, НЕ назначает и НЕ отменяет
   лечение, НЕ дозирует препараты. Роль сервиса — память, порядок в данных, динамика, подсказки
   по образу жизни (еда, спорт) и **список вопросов к врачу**. Любая аналитическая выдача по
   здоровью — со штампом «это не медицинский совет» и с явным «обсудить с врачом: …».
2. **Медданные не покидают периметр без явного решения PO.** `data/` — никогда в git.
   Внешние LLM-API не подключаем (решение PO: интерпретацию делает архитектор, читая БД
   напрямую). Тесты — **только на синтетических данных**; реальные выгрузки PO в тесты
   и фикстуры не вшиваем никогда.

## Данные: инварианты

- **`data/raw/**` — личные сырые данные, в git не попадают никогда** (закрыто `.gitignore`,
  держим так).
- **Импорт идемпотентен:** повторный импорт того же файла не плодит дубли (Этап 1; до его
  реализации — не гнать импорт одного файла дважды).
- **Единый словарь метрик** (`ingest/metrics.py`): одна и та же метрика из разных источников
  сводится к одному каноническому имени — иначе динамика не сравнится.
- **Источник правды по документу — сам файл** в `data/documents/`; в БД — извлечённый текст
  и ссылка на файл. PDF-конвейер — из reports (ADR-0001).

## Код и тесты — коротко

**Правку кода без зелёного `make test` не сдаём** (unit + e2e). Тесты в сеть не ходят, фикстуры
строятся на боевых моделях SQLAlchemy, а не на выдуманной схеме.

**Комментарии пишутся ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ** (контекст теряется, комментарий = опора): объясняют
ЗАЧЕМ и ПОЧЕМУ ИМЕННО ТАК, а не пересказывают код. Docstring — «зачем» модуля/функции, inline —
на неочевидном. Конвенция `AI:`: каждое новое нетривиальное место (инвариант, грабля, особый
случай реальных данных) помечаем `AI:` — `grep -rn "AI:" src/` обязан показывать ВСЕ опасные
места одной командой (живая карта мин). Грабли без тега не сдаём — проверяется на приёмке;
старое задним числом не ретрофитим. Плюс метка задачи (`# Э1.2`). Полное правило —
`docs/practices/код-и-тесты.md` §2.

**Всё разворачиваем в Docker** (исключение — физически привязанное к маку; см. практику).
Подробности — `docs/practices/код-и-тесты.md`.

## Роли, эпики, кодер — коротко

- **PO — Владимир** (продукт, стратегия, приёмка; системный аналитик, код не пишет).
  **Архитекторы** — Claude Opus, Kimi K3, Qwen 3.8Max — равны и взаимозаменяемы: один и тот же
  процесс для всех троих. **Кодер** — **DeepSeek** (через CLI, автономный цикл), руки нового
  кода; работает в отдельной рабочей копии на ветках `feat/*`, в `main` не мержит.
  **GLM больше не кодер** — не привлекать.
- **Формат работы с PO — КОЛЛЕГИАЛЬНЫЙ:** выдача = рекомендация с обоснованием, не вердикт;
  решение совместное. Архитектура и план — до кода.
- **Эпик = бизнес-функционал** (нужны бэк + фронт + тесты — всё в ОДНОМ эпике, вертикально).
  **Наряд = документ на эпик.** ⛔ Граница документов: `docs/План и реализация.md` = ЭПИКИ,
  `docs/наряды/` = ЗАДАЧИ. Новую задачу в план НЕ заводим.
- **Декомпозицию делает архитектор** — не PO и не кодер. Режим запуска («под ключ» / «пауза
  после батча») задаёт PO строкой в шапке наряда.
- **Сдача кодера — только зелёный гейт** (красный = возврат без разбора, два прохода
  архитектора максимум). Критерий передачи — `docs/practices/Роли и воркфлоу.md` §5.
- **Качество выше токенов**, но единица измерения — ЛИМИТ АРХИТЕКТОРА, не деньги
  (решение PO 11.08): кодер бесплатен по всем фронтам, расход ≈ контекст × запросы,
  экономим структурой сессии («фаза = сессия»), не урезая проверки. Приёмы и порог —
  `docs/наряды/README.md` §10.

Полностью — `docs/practices/Роли и воркфлоу.md`; механика нарядов — `docs/наряды/README.md`.

## Память архитектора

**В памяти — ТОЛЬКО состояние и правила поведения.** Фактура вся в git. Нашёл в памяти деталь,
которой место в доке, — перенеси в док, а не дублируй. **Медданные в память не пишем никогда.**
Перекрёстное чтение памяти Kimi/Qwen/Claude и пути — `AGENTS.md` §3. **Чужую память не править.**
